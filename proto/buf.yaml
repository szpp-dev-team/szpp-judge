version: v1
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
    - BASIC
  ignore_only:
    ENUM_VALUE_PREFIX:
      - judge/v1/resources.proto
      - backend/v1/task_resources.proto
deps:
  - buf.build/googleapis/googleapis
